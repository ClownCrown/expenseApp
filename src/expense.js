class Expense {
  constructor(id, header, price, desc, type) {
    this.id = id;
    this.header = header;
    this.price = price;
    this.desc = desc;
    this.type = type;
  }
}

export default Expense;
